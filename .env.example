# Clerk publishable key (required for `hookie login` in source builds)
CLERK_PUBLISHABLE_KEY=

# App URL for login redirect (optional; defaults to https://app.hookie.sh in prod builds).
# HOOKIE_APP_URL=http://localhost:3001

# Relay gRPC address (optional; official builds default to hosted relay)
# HOOKIE_RELAY_URL=
